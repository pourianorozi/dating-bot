# Dating Bot

Telegram dating bot with profile, search, like, match and chat features.

## Setup

```bash
pip install -r requirements.txt
export BOT_TOKEN=your_token
python main.py
```

## Security

Do not hardcode the bot token. Use the BOT_TOKEN environment variable.
